import { createAuthClient } from 'better-auth/react';

/**
 * Initializes the Better Auth client for React.
 *
 * This `authClient` instance provides React hooks and utilities
 * to manage authentication state, user sessions, and auth-related actions
 * on the client side.
 */
export const authClient = createAuthClient({});
